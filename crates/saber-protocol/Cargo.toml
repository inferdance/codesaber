[package]
name = "saber-protocol"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Single source of truth for saber wire protocol: RPC methods, EventMsg, config, and the session JSONL event schema."

[lints]
workspace = true

[dependencies]
