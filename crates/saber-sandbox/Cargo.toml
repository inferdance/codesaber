[package]
name = "saber-sandbox"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "macOS Seatbelt sandbox for tool subprocesses: workspace-write-lite profile, deny-read secrets globs, network deny, env allowlist."

[lints]
workspace = true

[dependencies]
saber-tools = { workspace = true }
futures = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
tempfile = { workspace = true }
