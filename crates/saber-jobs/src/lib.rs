//! Background jobs registry (M3): `start/list/read/kill/wait` semantics
//! with session-scoped ownership fencing and the "no attached controller,
//! no start" rule (dsh-style). Placeholder until M3.
