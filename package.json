{
  "name": "codesaber",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "tsc -b && tsc -p packages/core/tsconfig.tests.json && tsc -p packages/ai/tsconfig.tests.json",
    "saber": "tsx packages/cli/src/main.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "execa": "^9.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
